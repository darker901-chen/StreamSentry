# M6.5 Spec-Guardian Report - fail-open repositioning (governance audit)

- Date: 2026-07-09 (audit completed 2026-07-10)
- Auditor: spec-guardian
- Change set: staged diff on top of 409964e (git diff --cached), 13 files (+355/-198)
- Governing law: CLAUDE.md and SPEC.md AS AMENDED in this staged change set
- Authority record: reports/RULING-2026-07-09-fail-open.md (owner ruling, FINAL)

## 0. Authority verification - PASS

reports/RULING-2026-07-09-fail-open.md is staged, marked FINAL, records the
owner intent, explicitly supersedes the fail-closed blackout semantics of old
iron rule 1 (and the raw-black reservation in old rule 3), and explicitly
preserves: deterministic-only, opaque drawn masks, no-third-party-deps, GPLv2,
scope-lock mechanics, the 500ms staleness definition. The staged
CLAUDE.md/SPEC.md amendments match the ruling engineering translation. This
audit therefore applies the amended rules. No CLAUDE.md-vs-SPEC.md conflict
found (see QUESTION Q1 for one un-annotated Part 1 line).

## 1. Law-doc consistency - PASS (with Q1)

| Check | Evidence | Result |
|---|---|---|
| CLAUDE.md rule 1 = never disrupt / mask only on confidence / chip+log / allowlist mask-all exception | CLAUDE.md:7 | PASS |
| CLAUDE.md rule 3 = drawn masks opaque, solid-fill fallback for confident rects | CLAUDE.md:9 | PASS |
| CLAUDE.md architecture bullets updated (unverified -> render unmodified + chip + log; padding = placement tolerance) | CLAUDE.md:17-18 | PASS |
| No live blackout mandate anywhere in CLAUDE.md | full read | PASS |
| SPEC Part 1 historical lines carry superseded notes | SPEC.md:25, :26, :27, :40, :63 | PASS |
| SPEC Part 2 sec 2.7 present and complete (confidence rule, chip+log, allowlist exception, gate flip, acceptance-row changes) | SPEC.md:212-244 | PASS |
| 2.1 collateral (stale events now chip occurrences; heartbeat semantics unchanged, touch stays caller-free) | SPEC.md:96-102 | PASS |
| 2.3 allowlist failure = mask-all plate + chip, never black, never silent pass-through; consistent with rule-1 exception | SPEC.md:149-158 | PASS |
| 2.4 panic precedence = deliberate user action outranks health; panic plate stays + chip on top | SPEC.md:170-181, :268 | PASS |
| No live blackout mandate in SPEC Part 2 | full read | PASS |
| Part 1 cloak-doubt line un-annotated | SPEC.md:28 | QUESTION Q1 |

## 2. Code compliance with amended rule 1 (src/filter.c) - PASS on the render path; V1 on the watcher side

- Source renders through the filter chain in EVERY path: src/filter.c:289-294
  (enabled=false -> skip), :296-299 (zero-size -> skip), :362-370
  (process_filter_begin failure -> obs_source_skip_video_filter, chip + one-shot
  WARN when unverified or masks pending), :371 (normal begin/end). PASS
- No remaining full-frame fill: draw_solid callers = src/filter.c:385 only
  (per-rect fallback); no gs_clear anywhere in src/. The old full-black vec4
  fill is removed. PASS
- draw_status_chip has no background fill: src/filter.c:240-262 (texture draw
  only; top-left, 12px margin, width-capped at 40%). PASS
- Unverified frames draw the chip and log one WARN per engagement / one INFO
  on clear: src/filter.c:391-396, log_mode :264-277. PASS
- SS_MAP_INVALID keeps confidently mapped rects: src/filter.c:324-338 (loop no
  longer aborts; OK rects accumulate; frame flagged unverified with reason) and
  :373-389 (plates drawn even on flagged frames). Matches ARCHITECTURE.md:143.
  PASS
- Stale/absent heartbeat -> mapping skipped entirely (src/filter.c:318), so no
  stale-position masks are ever drawn (mask only on confidence). PASS
- process_filter_begin failure with confident rects pending: rects are NOT
  drawn (render state unreliable - drawing into a failed chain is not
  confident placement); chip + WARN instead. Matches the milestone definition
  and sec 2.7. PASS (noted)
- V1 (watcher side, see section 7): one protection-degradation path surfaces
  no notice at all - violates the tell-the-user half of amended rule 1.

## 3. Code compliance with amended rule 3 - PASS

- Plate texture failure -> solid opaque fallback: src/filter.c:380-388,
  vec4_set(dark, 0.08f, 0.09f, 0.11f, 1.0f) - alpha 1.0, drawn at the mapped
  rect, mask never dropped, WARN logged. PASS
- No blur / pixelation / transparency introduced anywhere in the diff. PASS
- Chip itself opaque: src/plate-gen.c:303-319 - alpha-255 fills covering the
  full extent, square corners, text alpha 255; only the label string changed
  ("StreamSentry: protection inactive - not masking"); structure unchanged.
  Banner opacity remains pixel-asserted by plate-gen-tests. PASS

## 4. Toast gate direction - PASS in code and tests; V1 on the missing log

- src/toast-gate.c:79-86: NULL/degenerate/non-finite inputs -> false (not a
  toast). PASS
- src/toast-gate.c:118-123: plausible_any with NULL/empty monitor list ->
  false. PASS
- src/watcher.cpp:293-309 call site and :389-398 incomplete-monitor guard:
  never evaluates the gate against partial monitor data; empties the list so
  the gate refuses to affirm. Comments match the flipped semantics. PASS
- tests/toast-gate-tests.c:112-127: every uncertainty assertion flipped to
  NOT-toast; the NaN fixture was strengthened to a window that would PASS on a
  sane monitor, so the short-circuit is actually proven. PASS
- BUT the safety-argument comment claims a mitigation that does not exist -
  see V1.

## 5. Unchanged invariants - PASS

- Deterministic only: no ML/AI anywhere in the diff. PASS
- Dependencies: no build-system or include changes; libobs + Windows SDK only.
  PASS
- Origin: all changes are small original edits in existing style; no evidence
  of imported/adapted code. PASS
- Threading: render side reads via pthread_mutex_trylock
  (src/shared-state.c:63-72), unchanged; chip = one-time lazy texture creation
  + per-frame sprite draw, same class as the removed fail-closed banner; heavy
  work stays on the watcher thread. PASS
- Heartbeat semantics: written only with a completed publish
  (src/watcher.cpp:418-423); ss_state_touch_heartbeat has zero callers in src/
  and tests/. PASS
- 500ms threshold: compile-time constant (src/filter.c:38); settings keys
  remain exactly {enabled, blocklist} - not user-configurable. PASS
- Chip not user-disableable: no setting gates it. The pre-existing 160x60
  geometry floor (src/filter.c:251) skips only the on-output chip on tiny
  sources; the log still fires; documented in ARCHITECTURE.md:177-179. PASS
- Blocklist matching untouched: src/watcher.cpp:311-320 (case-insensitive
  substring vs process image name OR title, ruling a434b18). PASS
- Scope = sec 2.7 only: no backlog items, no new features, no new settings.
  TESTING.md/CHANGELOG.md untouched is correct at this stage - per
  ARCHITECTURE.md:238-250 the scribe writes them after this gate; the scribe
  must record the amended acceptance rows (SPEC.md:63, :240-244). PASS

## 6. Explicit off-switch paths draw no chip - PASS

src/filter.c:289-294 (!enabled) and :296-299 (!w || !h): skip_video_filter,
no chip, no log. Documented as deliberate non-triggers in
ARCHITECTURE.md:151-156. An explicit user off-switch is not a failure state.
PASS

## 7. VIOLATIONS

V1 - Silent toast-protection degradation without the mandated failure notice
(amended CLAUDE.md rule 1; SPEC.md:222-228 "the condition is logged").
src/watcher.cpp:389-398: monitor-enumeration failure or truncation (>16
monitors, GetMonitorInfoW failure, EnumDisplayMonitors failure) empties the
monitor list; the flipped gate then classifies EVERY toast-signature match as
not-a-toast, so toast masking silently stops. No obs_log call exists on this
path (all watcher log sites: src/watcher.cpp:439, 443, 446, 449, 470, 488,
516, 549, 641, 643 - none cover it), and the heartbeat stays fresh, so no
chip and no log - the render side cannot know. src/toast-gate.c:58-60 asserts
this residual "is logged by the watcher's degraded paths": that claim is
FALSE as staged, which also invalidates the SPEC-2.2-required safety argument
as written. Under the old law this path failed safe (over-mask); the
direction flip is correct per the ruling, but the ruling's own condition -
tell the user - was not implemented. A new silent fail-open path: forbidden.

V2 - Live user-facing blackout claim in README.
README.md:25-27: "covers them with an opaque plate, or - if it can't verify
it's healthy - blacks the entire source." Contradicts amended rule 1, the
ruling's own deliverable ("README claims updated minimally now" - this is the
core claim), and README.md:37-48 two paragraphs below it.

V3 - Live log strings still assert blackout behavior.
src/watcher.cpp:549-550: LOG_ERROR "detection unavailable (render side will
fail closed)" - false and over-reassuring: the render side now shows the
source UNMASKED with a chip; this message tells the user the opposite of the
truth about their protection state, the precise failure mode the ruling
targets. src/watcher.cpp:470-473: LOG_WARNING "(early warning; fail-closed
stale threshold is 500 ms)". Runtime log messages are user-facing behavior,
not comments.

V4 - ARCHITECTURE.md (staged) contradicts itself and the amended law.
CLAUDE.md:14 incorporates this file by reference. Stale lines mandating
superseded semantics: ARCHITECTURE.md:19 ("SS_MAP_INVALID, which callers must
treat as fail-closed" - contradicts its own :143), :20 ("caller fails
closed" - contradicts :142), :23 ("fail-closed rendering"), :45-47 ("render
side fails closed" - contradicts :140), :21 ("fail-closed status banner"),
and :194 - the test table claims toast-gate-tests prove "uncertainty
classifies as toast" when the staged tests prove exactly the opposite.

V5 - Enforcement config still mandates the superseded law.
.claude/agents/spec-guardian.md:20-21: "Fail-closed integrity: every new
failure path must land in blackout, never fail-open; fail-closed must not be
user-disableable." This milestone's own ARCHITECTURE.md:232-250 formalizes
the agent definitions as the documented workflow whose rulings "bind all
later audits"; a future gate run following that bullet verbatim would demand
violations of amended rule 1. Flagged for the OWNER: governance/agent config
changes require owner authority - no agent should edit it unilaterally, and
this audit modified nothing.

## 8. QUESTION for the human (not counted in the verdict)

Q1 - Cloak-check doubt direction. SPEC.md:28 still reads "Check DWMWA_CLOAKED
+ visibility before reporting, but when in doubt, report (over-mask)" with NO
superseded note, and src/watcher.cpp:208-214 keeps the matching behavior
(DwmGetWindowAttribute failure -> treated as not cloaked -> window proceeds
to matching). The ruling flipped only the geometry gate. Is this retained
deliberately (the rect is exact; only visibility is uncertain) or should it
flip too (masking a genuinely cloaked window would plate over innocent
content = wrong mask)? Either way the Part 1 convention requires an inline
note on SPEC.md:28, since its stated over-mask rationale cites the superseded
rule.

## 9. Non-blocking cleanup (stale references to the superseded law in
comments / process docs; none change behavior)

- src/filter.c:146-148 ("NO option to disable the fail-closed behavior" -
  should now name the chip), :289-291 ("Fail-closed cannot be disabled while
  enabled")
- src/coord-map.h:60, :67 (contract comments: "caller must fail closed")
- src/geom-resolve.h:42 ("fail closed if there are rects to mask")
- src/watcher.h:49 ("for the fail-closed test")
- src/watcher.cpp:62-63; :72-75 (cites old iron rule 1 over-mask rationale,
  pre-dates the gate); :130-133; :466-468; :501-502
- tests/watcher-selftest.cpp:174 (comment; the CHECK message itself was
  updated); tests/plate-gen-tests.c:86 ("fail-closed banner")
- SETUP.md:38, :81 (zh-TW process notes describing blackout-era manual
  acceptance; historical document - annotate at will)
- ARCHITECTURE.md:19, :116 "over-mask" phrasing for padding (amended law
  frames it as placement tolerance around a confident detection)

## Verdict: FAIL

V1 is a behavioral gap in amended rule 1's notice requirement (a new silent
fail-open path plus a false safety-argument comment). V2-V5 are live
contradictions of the amended law in user-facing and governing text - for a
milestone whose entire purpose is the repositioning, they are its subject
matter, not collateral. The render-path core (sections 2, 3, 6), the gate
flip in code and tests (section 4), and all unchanged invariants (section 5)
otherwise comply; the fix scope is narrow and well-localized.

---

# RE-AUDIT (2026-07-10) — after fixes for V1-V5 and Q1

The first-pass text above is preserved verbatim as evidence. The
coordinator reported all five violations and Q1 addressed; the staged
diff was re-audited end-to-end against the amended law (change set now
21 files, +808/-239; reports/M6.5-verifier.md: VERIFIED, including a
run-2 clean re-verification of the post-audit tree).

## Authority note on the fixes themselves

reports/RULING-2026-07-09-fail-open.md gained an "Application addendum
(2026-07-10)". Assessment: the original FINAL ruling text (lines 1-52)
is intact; the addendum is APPENDED, clearly dated and labeled as the
implementer applying the ruling's recorded principles, with the
authority chain stated per item — it does not put new words in the
owner's mouth. The file is still uncommitted (new in this change set),
so the "evidence is committed verbatim and never edited after the
fact" rule (ARCHITECTURE.md:249-250) is not violated. The two
derivative decisions (cloak-doubt direction, guardian-checklist
amendment) are direct applications of ruling items 1-2/4 and remain
subject to the owner's manual acceptance gate, which this milestone
still requires to land (ARCHITECTURE.md:245-246). Accepted on that
basis and flagged for the owner's acceptance pass.

## Resolution verification

| First-pass finding | Fix as staged | Status |
|---|---|---|
| V1 silent toast-gate degradation | ss_snapshot gains detection_degraded (src/shared-state.h:54-64, documented); watcher publishes it every tick (src/watcher.cpp:443) and logs engage/clear transitions once each (src/watcher.cpp:408-419, g_mons_degraded, watcher-thread-only); filter consumes it -> chip + reason "detection degraded (monitor data unavailable)" (src/filter.c:309-315); toast-gate.c:57-63 safety argument now truthfully describes flag+log; SPEC.md:229-232 codifies "no degradation is ever silent" | NOTICE RESOLVED, but the filter-side wiring introduced V6 (below) |
| V2 README blackout claim | README.md:25-27 rewritten (chip language, no blackout claim); README.md:43 label updated | RESOLVED |
| V3 blackout log strings | src/watcher.cpp:573 now "render side will show the protection-degraded chip"; :489-493 now "detection-stale threshold is 500 ms" | RESOLVED |
| V4 ARCHITECTURE.md contradictions | :19 (INVALID -> no mask for that detection + DEGRADED frame), :20 (renders DEGRADED), :21 (failure status chip), :23 (DEGRADED-chip rendering), :45-47 (goes DEGRADED with "no detection snapshot"), :195 (test row now "uncertainty classifies as NOT a toast"); failure table gains trigger row #3 for detection_degraded (:142) | RESOLVED (but see V6: code contradicts the new row #3) |
| V5 guardian checklist mandates blackout | .claude/agents/spec-guardian.md bullet replaced by failure-notice integrity: never disrupt output / never guess / never silent / chip+log not user-disableable / allowlist fails to mask-all / confident masks never dropped (solid-fill fallback). Faithful to amended rules 1+3, cites the ruling record. Authority recorded in the ruling addendum item 4 | RESOLVED (owner countersign at manual acceptance) |
| Q1 cloak-doubt direction | is_cloaked query failure -> treat as cloaked, skip (src/watcher.cpp:215-223, rationale in-code); SPEC.md:28 now carries the SUPERSEDED note matching it | RESOLVED. Residual recorded: a genuinely visible window whose DWMWA_CLOAKED query persistently fails is skipped per-window without a notice. Accepted rationale: the dominant real-world failure is a window handle dying mid-enumeration (nothing left to protect), DWM is always-on for the supported OS range, and a per-window log would spam. The owner sees this note at acceptance |

Label consistency check: plate-gen.c:305 "StreamSentry: protection
degraded - see log" = plate-gen.h:52-55 comment = SPEC.md:226 =
ARCHITECTURE.md:131 = README.md:43 (short form). Nit: SPEC/ARCH quote
the label with a typographic em dash, the bitmap-font code renders an
ASCII hyphen. Cosmetic.

CLAUDE.md is unchanged from the first pass and remains clean. Iron
rules 2/4/5/6 re-checked on the delta: no ML, no new deps/includes, no
new features (detection_degraded is the mandated failure notice for an
existing path, not a feature), license headers intact. Threading:
the flag rides inside the existing snapshot (same trylock read); the
transition log runs on the watcher thread. Heartbeat: still written
only with a completed publish; ss_state_touch_heartbeat still has zero
callers. tests/toast-gate-tests.c unchanged from first pass (still
asserts the flipped direction). All PASS.

## NEW VIOLATION found in the re-audit

**V6 - detection_degraded drops confidently detected and mapped masks
(amended rule 1 / SPEC 2.7 "Confident masks keep all v0.1 guarantees";
ruling: "Confidently detected and mapped threats are still masked").**
src/filter.c:312-315 folds the flag into `unverified` BEFORE the
mapping block, and src/filter.c:326 gates all mapping on
`!unverified` - so while monitor data is unavailable, num_mapped stays
0 and NO plates are drawn at all. But a monitor-enumeration failure
degrades ONLY the toast gate: blocklist and password-field detections
in the same snapshot are exact (EnumWindows/GetWindowRect/UIA
unaffected) and render-side geometry resolution is independent
(geom-resolve does its own monitor lookup and has its own trigger).
Dropping those confident masks is unjustified under-masking - the very
regression the ruling guarded against. The staged documentation
already promises the correct behavior: ARCHITECTURE.md:142 trigger row
#3 says "block/allowlist + password masks still active", and the
amended guardian checklist itself says "confident masks are never
dropped". Doc matches the law; the code is the bug
(ARCHITECTURE.md:8). In-file precedent for the correct wiring is the
SS_MAP_INVALID branch (src/filter.c:340-344): flag the frame, keep
mapping. The fix is to surface the chip+reason for detection_degraded
WITHOUT gating the mapping block on it. Not caught by the verifier:
no automated test covers masking behavior under detection_degraded
(ctest targets are pure units; the selftest exercises kill/stale
only) - a regression test here would be appropriate.

## Non-blocking cleanup remaining (comments/phrasing only)

- src/filter.c:289-291: render-path comment still says "Fail-closed
  cannot be disabled while enabled" (the properties comment :146-148
  WAS fixed)
- tests/watcher-selftest.cpp:174 comment "> 500ms fail-closed
  threshold"; tests/plate-gen-tests.c:86 "fail-closed banner"
- ARCHITECTURE.md:206 "protection-inactive chip" (label since
  generalized to "protection degraded")
- SETUP.md:38, :81 (historical zh-TW process notes, unchanged)
- watcher.cpp:137 now frames fail-closed historically ("v0.1 ... (now
  the degraded chip)") - acceptable as history, no action needed

## Re-audit verdict: FAIL

One violation remains: V6. Everything else from the first pass is
resolved, the authority chain for the law amendment and its
applications is properly recorded, and the fix for V6 is small and
fully specified by the project's own staged documentation
(ARCHITECTURE.md:142).

---

# ROUND 3 (2026-07-10) — V6 fix verification

Change set now 26 files, +1355/-273. Delta since round 2: new pure
module src/frame-decide.c/.h, filter.c refactored onto it, new ctest
suite tests/frame-decide-tests.c, CMakeLists.txt wiring, the four
non-blocking cleanups, ARCHITECTURE.md rows. SPEC.md / CLAUDE.md /
README.md / agents / ruling record are byte-identical to the round-2
audited state (verified via diff stat).

## V6 — RESOLVED (structurally)

- src/frame-decide.c:51-52: detection_degraded flags the frame but
  does NOT gate anything — execution falls through to geometry +
  mapping, so confident rects keep their masks. The exact failure of
  round 2 is now impossible by construction, and the module comment
  cites the finding (src/frame-decide.h:19-33).
- Trigger order implemented = ARCHITECTURE.md:136-145 table exactly:
  no-snapshot / stale (no masks — stale rects untrusted, correct under
  mask-only-on-confidence) -> degraded (masks continue) -> geometry
  unresolved (no masks) -> per-rect OK/NOT_VISIBLE/INVALID (keep OK
  rects). flag() keeps the FIRST reason (src/frame-decide.c:21-28);
  degraded-over-mapping precedence matches table numbering. Stale
  beats degraded (whole snapshot untrusted) — consistent with table
  order #2 < #3.
- Module purity: includes only coord-map.h + shared-state.h + libc
  headers; no OBS, no Windows, no allocation, no locks — safe for the
  render thread and unit-testable (iron rule 4 untouched; GPL headers
  present on all three new files).
- filter.c:28 includes the module; :309-321 resolves geometry only for
  a fresh snapshot with rects (including degraded frames, as mask
  continuity requires) and delegates the decision; drawing loop,
  solid-opaque fallback (:361-369), chip (:372-374), one-shot logging,
  and the chain-bypass path (:343-351) all preserved from the round-2
  audited state. enabled=false / zero-size still skip chip-free
  (:290-301) with the comment now stating the correct rule.
- tests/frame-decide-tests.c (registered: CMakeLists.txt:83,:87; links
  only pure sources): 9 cases assert the law semantics with exact
  reason strings — headline V6 regression (degraded + confident WINDOW
  rect + valid geometry -> unverified AND num_mapped==1, :82-91),
  degraded+zero rects (chip only), no-snapshot, stale-drops-all,
  geometry-unresolved, INVALID-among-OK keeps the OK mask and its
  kind, off-capture silent skip, degraded-over-mapping reason
  precedence. The reason strings tested are the ones ARCHITECTURE's
  trigger table documents — doc/code/test now pin each other.

## Cleanup verification

- src/filter.c:290-293 enabled-off comment fixed ("not a failure
  state, so no chip. The chip cannot be disabled while the filter is
  enabled.") — DONE
- tests/watcher-selftest.cpp:174 ("detection-stale threshold") — DONE
- tests/plate-gen-tests.c:86-87 ("status chip ... every single pixel
  must be opaque") — DONE, opacity assertion unchanged
- ARCHITECTURE.md:206-area "protection-inactive" phrasing — DONE
  (case-insensitive sweep of src/ and tests/ finds no live fail-closed
  text; watcher.cpp:137 frames it historically, which is correct)

## Remaining nits (non-blocking, no superseded semantics asserted)

- ARCHITECTURE.md:28-32: dependency-direction paragraph omits
  frame-decide from filter.c's dependency set and still says "the two
  pure modules (coord-map, plate-gen)" — there are now four (toast-gate
  M6, frame-decide M6.5), and frame-decide is precisely the module
  whose purity makes the V6 security property testable. The module
  table two lines above (:23) states it correctly.
- ARCHITECTURE.md:89-105 data-flow diagram: semantically consistent
  but does not show the frame-decide boundary or the
  detection_degraded field in the published snapshot (diagram is
  scoped "one healthy frame"; the failure table covers it).
- SPEC.md:101, :264: "protection-inactive" used as an adjective in
  §2.1/acceptance rows; the label is now "protection degraded — see
  log" (SPEC.md:226 quotes it correctly). Descriptive text, not a
  label quote.

## Workflow note for the hub/scribe (not a spec violation)

reports/M6.5-verifier.md's authoritative ADDENDUM pins its VERIFIED
verdict to the ROUND-2 tree identity ("this addendum is authoritative
for the final staged tree" — that tree predates frame-decide,
CMakeLists changes, and the filter refactor; the report nowhere
mentions frame-decide). Per the documented gate rules
(ARCHITECTURE.md:241-243) the scribe must not record until the
verifier re-runs on THIS tree (4 suites + selftest + build). The
coordinator reports all green; the verifier's own record of that runs
the gate, not this report.

Standing items for the owner's manual acceptance pass (from round 2):
countersign the ruling's Application addendum (cloak-doubt direction,
chip-label generalization, guardian-checklist amendment) and the Q1
per-window residual note.

## ROUND-3 VERDICT: PASS

All first-pass violations (V1-V5), the round-2 violation (V6), and Q1
are resolved in the staged tree; the resolution of V6 is enforced by a
dedicated unit suite rather than review alone. No new violations.
Remaining items are the three documentation nits above (non-blocking)
and the stale-verifier workflow note, which gates the scribe, not this
audit.
