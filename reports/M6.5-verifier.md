# M6.5 Verifier Report — fail-open repositioning (owner ruling 2026-07-09)

- Run: 2026-07-09, local machine (Windows 11 Home 10.0.26200)
- Tree under test: HEAD `409964e` + staged M6.5 diff (13 files, +355/−198), no unstaged changes
- Toolchain: CMake 3.28.0-rc5, generator "Visual Studio 17 2022" x64, preset `windows-x64-local`, config RelWithDebInfo, dependency cache `.deps` present
- **Verdict: VERIFIED — in runs 1, 2 and 3 (each a clean re-verification after a tree amendment; the RUN-3 ADDENDUM at the end is authoritative for the final staged tree)**

## 0. Scope of the staged diff (verified present)
Code: `src/filter.c`, `src/plate-gen.c/.h`, `src/toast-gate.c/.h`, `src/watcher.cpp`;
tests: `tests/toast-gate-tests.c` (uncertainty expectations flipped), `tests/watcher-selftest.cpp` (kill-leg message);
law/docs: `CLAUDE.md` (iron rules 1 and 3 rewritten), `SPEC.md` (new §2.7 at line 212; v0.1 clauses annotated SUPERSEDED, not deleted), `ARCHITECTURE.md`, `README.md`;
new: `reports/RULING-2026-07-09-fail-open.md`.

Consistency check: the staged CLAUDE.md/SPEC.md changes match the ruling document's
"engineering translation" point-for-point (blackout removed; render-unmodified + status
chip on unverified state; solid-fill fallback for confident rects; toast-gate uncertainty
→ not-a-toast; allowlist failure → mask-all reserved for M7). The toast-gate test diff
inverts exactly the five uncertainty CHECKs (no monitors, zero count, null monitor,
degenerate monitor, NaN monitor) and adjusts the NaN fixture rect (1080→2148 x) so the
case stays discriminating under the new rule.

## 1. Commands executed (verbatim)
```
rm -rf F:/obsplugin/build_x64
cmake --preset windows-x64-local
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=ON
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=OFF
cmake --build --preset windows-x64-local
ctest -C RelWithDebInfo --output-on-failure          # cwd: F:/obsplugin/build_x64
./coord-map-tests.exe; ./plate-gen-tests.exe; ./toast-gate-tests.exe
./watcher-selftest.exe                               # cwd: build_x64/RelWithDebInfo
```

## 2. Build results — PASS, zero warnings
| Step | Exit | Warnings/Errors in log |
|---|---|---|
| Clean configure (PERF_LOG default OFF) | 0 (22.4 s) | 0 |
| Build RelWithDebInfo, PERF_LOG=OFF | 0 | 0 |
| Reconfigure + build, PERF_LOG=ON | 0 | 0 (cache: `STREAMSENTRY_PERF_LOG:BOOL=ON`) |
| Restore OFF, reconfigure + build | 0 | 0 (cache: `STREAMSENTRY_PERF_LOG:BOOL=OFF` — tree left OFF) |

Warning scan: `grep -c -iE "warning|警告|error C|錯誤"` = 0 on every build log
(MSBuild runs in the zh-TW locale here; both English and localized patterns checked).

Build tail (OFF build):
```
  streamsentry.vcxproj -> F:\obsplugin\build_x64\RelWithDebInfo\streamsentry.dll
  coord-map-tests.vcxproj -> F:\obsplugin\build_x64\RelWithDebInfo\coord-map-tests.exe
  plate-gen-tests.vcxproj -> F:\obsplugin\build_x64\RelWithDebInfo\plate-gen-tests.exe
  toast-gate-tests.vcxproj -> F:\obsplugin\build_x64\RelWithDebInfo\toast-gate-tests.exe
  watcher-selftest.vcxproj -> F:\obsplugin\build_x64\RelWithDebInfo\watcher-selftest.exe
```

Cosmetic note (not a warning, pre-existing, not part of this diff): the template's
dependency bootstrap prints a typo during configure — "Build OBS sources (Reelase - x64)".

## 3. Test results
### ctest (RelWithDebInfo) — 3/3 suites PASS
```
1/3 Test #1: coord-map-tests ..................   Passed    0.02 sec
2/3 Test #2: plate-gen-tests ..................   Passed    0.14 sec
3/3 Test #3: toast-gate-tests .................   Passed    0.02 sec
100% tests passed, 0 tests failed out of 3
```
Direct runs all exit 0 ("all passed"). Static CHECK() assertion counts in the sources:
coord-map 35, plate-gen 24, toast-gate 23.

### watcher-selftest.exe — all deterministic checks PASS (exit 0)
Not ctest-registered by design (launches notepad, fires a toast). Verbatim output:
```
ok:   watcher thread started
watcher thread started (tick 150ms)
ok:   heartbeat advances while watcher alive
info: baseline blocklist rects = 0
ok:   launched notepad.exe
ok:   blocklist rect appeared after notepad opened
ok:   blocklist rect disappeared after notepad closed
watcher: DEBUG kill engaged (heartbeat frozen)
ok:   heartbeat frozen while killed (>500ms) -> render shows protection-inactive chip
watcher: DEBUG kill released
ok:   heartbeat resumes after un-kill
INCONCLUSIVE: no toast banner detected (likely Do Not Disturb / focus assist suppressing the banner) - see reports/M2-DECISIONS.md; real toast masking is on the human checklist
watcher thread stopped
ok:   watcher thread stopped cleanly
watcher-selftest: all deterministic checks passed
```
The INCONCLUSIVE toast leg is expected on this machine — toast banners are
system-suppressed here (reports/M6-toast-probe.txt); it is not a regression and does not
gate the verdict. The kill-leg message is the new M6.5 string (chip, not blackout).

## 4. Artifacts — present
`F:\obsplugin\build_x64\RelWithDebInfo\` (all timestamped this run):
`streamsentry.dll` (70,144 B) + `streamsentry.pdb`, `coord-map-tests.exe`,
`plate-gen-tests.exe`, `toast-gate-tests.exe`, `watcher-selftest.exe`
(+ `w32-pthreads.dll` copied beside the self-test, per CMake post-build rule).

Informational (per task instruction — noted, not failed): a perf-instrumented fail-open
DLL was deployed tonight for the owner's soak at
`D:\software\obs\obs-studio\obs-plugins\64bit\streamsentry.dll` (72,704 B, 23:49) + pdb.
The size delta vs the fresh PERF_LOG=OFF build (70,144 B) is consistent with
instrumentation being compiled in there.

## 5. Not covered by automation (explicit)
1. **In-OBS chip behavior** — source keeps rendering unmodified with a small opaque
   "protection inactive" chip within ≤500 ms of watcher kill; chip clears on recovery;
   OBS log lines emitted. Owner-manual item (SPEC.md manual matrix as amended by §2.7).
2. **M6 soak rows** — no mask-storm during normal desktop use; a real toast being masked
   inside OBS (banners are system-suppressed on this machine, so no automated coverage).
3. **Allowlist-mode failure semantics** (§2.7 fail-toward-mask-all) — M7, not yet
   implemented; nothing to test.
4. **Render-path code** (`src/filter.c` graphics calls, chip drawing, solid-fill
   fallback) has no unit harness; only the pure modules (coord-map, plate-gen buffer
   logic, toast-gate) are unit-tested. The heartbeat/kill state machine is covered by
   the self-test; the pixels it produces are not.
5. **Governance provenance** — `reports/RULING-2026-07-09-fail-open.md` exists and the
   staged law-doc changes are internally consistent with it, but this verifier cannot
   machine-verify that the document faithfully records the owner's decision. That
   attestation belongs to the owner/scribe record, not to build automation. Noted
   because the diff inverts former iron rule 1 (fail-closed blackout), which the
   pre-M6.5 CLAUDE.md marked never-violate.

## 6. Verdict
**VERIFIED** — clean zero-warning RelWithDebInfo builds in both PERF_LOG variants (tree
left at default OFF), 3/3 ctest suites pass, watcher self-test deterministic legs all
pass with the expected INCONCLUSIVE toast leg, all artifacts present.

---

# ADDENDUM — post-audit tree amendment and clean re-verification (run 2 — SUPERSEDED by run 3)

## What happened
After the run-1 VERIFIED above, the spec-guardian's first audit FAILED with findings
that required code changes (coordinator notification; audit record:
`reports/M6.5-spec-guardian.md`). The staged tree grew from 13 files (+355/−198) to
21 files (+808/−239). Run 2 re-executed the full standard sequence from scratch against
the amended tree on 2026-07-10, 00:10–00:20 local. Sections 1–4 above describe the
superseded run-1 tree; **this addendum is authoritative for the final staged tree.**

## Delta since run 1 (coordinator description, spot-verified in tree)
- `src/shared-state.h` — snapshot gains `bool detection_degraded` (line 61).
- `src/watcher.cpp` — publishes the flag (line 443: `snap.detection_degraded =
  (ctx.num_mons == 0)`), transition logs, cloak-query-failure now skips the window,
  comment/string updates.
- `src/filter.c` — consumes the flag (line 312); failure log prefix now
  `"PROTECTION DEGRADED: ..."` (line 271).
- `src/plate-gen.c/.h` — chip label now `"StreamSentry: protection degraded - see log"`.
- `src/toast-gate.c` (comment), `src/coord-map.h`, `src/geom-resolve.c/.h`,
  `src/watcher.h` — comment/doc-level updates.
- `tests/watcher-selftest.cpp` — kill-leg message now "render shows protection-degraded
  chip". `tests/toast-gate-tests.c` unchanged from run 1 (blob e184194).
- No CMake changes (CMakeLists.txt / presets absent from the staged list) — confirmed.

Tree identity for this addendum (staged blobs, working tree == index):
`src/filter.c 68f851c · src/watcher.cpp 1857a01 · src/shared-state.h 58a5205 ·
src/plate-gen.c 5a76e0f · src/toast-gate.c 23c8466 · tests/watcher-selftest.cpp 56fb620 ·
tests/toast-gate-tests.c e184194` (HEAD still `409964e`).

## Commands executed (verbatim, identical sequence to run 1)
```
rm -rf F:/obsplugin/build_x64
cmake --preset windows-x64-local
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=ON
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=OFF
cmake --build --preset windows-x64-local
ctest -C RelWithDebInfo --output-on-failure          # cwd: F:/obsplugin/build_x64
./coord-map-tests.exe; ./plate-gen-tests.exe; ./toast-gate-tests.exe
./watcher-selftest.exe                               # cwd: build_x64/RelWithDebInfo
```

## Run-2 build results — PASS, zero warnings
| Step | Exit | Warnings/Errors in log |
|---|---|---|
| Clean configure (PERF_LOG default OFF) | 0 | 0 |
| Build RelWithDebInfo, PERF_LOG=OFF | 0 | 0 |
| Reconfigure + build, PERF_LOG=ON | 0 | 0 (cache ON; dll 73,216 B) |
| Restore OFF, reconfigure + build | 0 | 0 (cache: `STREAMSENTRY_PERF_LOG:BOOL=OFF` — tree left OFF) |

Same warning scan as run 1 (`warning|警告|error C|錯誤`, zh-TW MSBuild locale): 0 hits
in every log.

## Run-2 test results
ctest RelWithDebInfo — 3/3 suites PASS:
```
1/3 Test #1: coord-map-tests ..................   Passed    0.02 sec
2/3 Test #2: plate-gen-tests ..................   Passed    0.14 sec
3/3 Test #3: toast-gate-tests .................   Passed    0.02 sec
100% tests passed, 0 tests failed out of 3
```
Direct runs: all three exit 0 ("all passed").

watcher-selftest.exe — all deterministic checks PASS (exit 0), verbatim:
```
ok:   watcher thread started
watcher thread started (tick 150ms)
ok:   heartbeat advances while watcher alive
info: baseline blocklist rects = 0
ok:   launched notepad.exe
ok:   blocklist rect appeared after notepad opened
ok:   blocklist rect disappeared after notepad closed
watcher: DEBUG kill engaged (heartbeat frozen)
ok:   heartbeat frozen while killed (>500ms) -> render shows protection-degraded chip
watcher: DEBUG kill released
ok:   heartbeat resumes after un-kill
INCONCLUSIVE: no toast banner detected (likely Do Not Disturb / focus assist suppressing the banner) - see reports/M2-DECISIONS.md; real toast masking is on the human checklist
watcher thread stopped
ok:   watcher thread stopped cleanly
watcher-selftest: all deterministic checks passed
```
Toast leg INCONCLUSIVE — same machine-level banner suppression as run 1
(reports/M6-toast-probe.txt); not a regression, does not gate the verdict. The kill-leg
string is the post-audit wording ("protection-degraded chip").

## Run-2 artifacts — present
`F:\obsplugin\build_x64\RelWithDebInfo\` (2026-07-10 00:14–00:15):
`streamsentry.dll` (70,656 B, final PERF_LOG=OFF build) + `streamsentry.pdb`,
`coord-map-tests.exe`, `plate-gen-tests.exe`, `toast-gate-tests.exe`,
`watcher-selftest.exe`, `w32-pthreads.dll`.

Deployment note (informational, per coordinator): the soak DLL at
`D:\software\obs\obs-studio\obs-plugins\64bit\streamsentry.dll` was refreshed tonight
(2026-07-10 00:12, 73,216 B + pdb). Corroboration: its size is byte-equal to this run's
PERF_LOG=ON build of the final tree, and it contains the post-audit strings
("protection degraded - see log" chip label and "PROTECTION DEGRADED" log prefix —
grep -a hits = 1 each). Byte-level provenance beyond that is not machine-verified here.

## Addendum to coverage gaps
Everything in the run-1 "Not covered by automation" section applies unchanged. Added by
this delta and likewise NOT covered by automation:
- The `detection_degraded` path end-to-end (zero-monitor condition → flag → chip while
  rendering continues) — the flag publishing/consumption compiles and the modules' unit
  tests pass, but no automated harness drives a zero-monitor snapshot through the OBS
  render path; in-OBS behavior is an owner-manual item.
- The watcher's cloak-query-failure skip and the degraded-transition log lines live in
  the Win32 enumeration path — verified by inspection and compilation only (the
  self-test exercises the enumeration loop but cannot force a DwmGetWindowAttribute
  failure).

## RUN-2 VERDICT (for the run-2 tree; superseded by the run-3 addendum): VERIFIED
Clean zero-warning builds in both PERF_LOG variants (tree left at default OFF), ctest
3/3 PASS, watcher self-test deterministic legs all PASS with the expected INCONCLUSIVE
toast leg, artifacts present, soak deployment refresh corroborated.

---

# RUN-3 ADDENDUM — V6 remediation tree and clean re-verification (AUTHORITATIVE for the final staged tree)

## What happened
The spec-guardian's re-audit found **V6: the degraded flag dropped confident masks**
(coordinator notification). Remediation landed after run 2: the render decision was
extracted from `src/filter.c` into a new pure module `src/frame-decide.c/.h`, and a
fourth ctest suite `tests/frame-decide-tests.c` (176 lines) adds V6 regression
coverage. Run 3 re-executed the full standard sequence from scratch on 2026-07-10,
00:23–00:30 local. **This addendum supersedes runs 1 and 2 and is authoritative.**

## Delta since run 2 (coordinator description, spot-verified in tree)
- NEW `src/frame-decide.c/.h` — pure decision module; `src/filter.c` now calls
  `ss_decide_frame()` (filter.c:320).
- NEW `tests/frame-decide-tests.c` — 25 CHECK assertions; headline case at line 82:
  `detection_degraded` + confident rect → chip WITHOUT dropping the mask (V6
  regression), plus reason-precedence ordering cases.
- `CMakeLists.txt` — module added to plugin sources (lines 51–52); new test target
  `frame-decide-tests` (links `src/frame-decide.c` + `src/coord-map.c`, line 83) and
  `add_test` registration (line 87).
- Comment-level cleanups: `tests/plate-gen-tests.c`, `tests/watcher-selftest.cpp`,
  ARCHITECTURE.md rows.
- `src/watcher.cpp` (1857a01) and `src/shared-state.h` (58a5205) blobs unchanged from
  run 2. Staged diff now 26 files, +1355/−273 (HEAD still `409964e`).

Tree identity for this addendum (staged blobs, working tree == index):
`CMakeLists.txt a3e94f5 · src/filter.c 649e298 · src/frame-decide.c 5ed1f37 ·
src/frame-decide.h 25ea4ca · tests/frame-decide-tests.c aaac788 ·
src/watcher.cpp 1857a01 · src/shared-state.h 58a5205`.

## Commands executed (verbatim, same sequence as runs 1–2; ctest now expects 4 suites)
```
rm -rf F:/obsplugin/build_x64
cmake --preset windows-x64-local
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=ON
cmake --build --preset windows-x64-local
cmake --preset windows-x64-local -DSTREAMSENTRY_PERF_LOG=OFF
cmake --build --preset windows-x64-local
ctest -C RelWithDebInfo --output-on-failure          # cwd: F:/obsplugin/build_x64
./coord-map-tests.exe; ./plate-gen-tests.exe; ./toast-gate-tests.exe; ./frame-decide-tests.exe
./watcher-selftest.exe                               # cwd: build_x64/RelWithDebInfo
```

## Run-3 build results — PASS, zero warnings
| Step | Exit | Warnings/Errors in log |
|---|---|---|
| Clean configure (PERF_LOG default OFF) | 0 | 0 |
| Build RelWithDebInfo, PERF_LOG=OFF | 0 | 0 (frame-decide.c + frame-decide-tests.exe in link output) |
| Reconfigure + build, PERF_LOG=ON | 0 | 0 (dll 73,216 B) |
| Restore OFF, reconfigure + build | 0 | 0 (cache: `STREAMSENTRY_PERF_LOG:BOOL=OFF` — tree left OFF) |

Same warning scan as prior runs (`warning|警告|error C|錯誤`, zh-TW MSBuild locale):
0 hits in every log.

## Run-3 test results — 4/4 suites PASS
```
1/4 Test #1: coord-map-tests ..................   Passed    0.02 sec
2/4 Test #2: plate-gen-tests ..................   Passed    0.14 sec
3/4 Test #3: toast-gate-tests .................   Passed    0.02 sec
4/4 Test #4: frame-decide-tests ...............   Passed    0.02 sec
100% tests passed, 0 tests failed out of 4
```
Direct runs: all four exit 0 ("all passed"). Static CHECK() counts: coord-map 35,
plate-gen 24, toast-gate 23, frame-decide 25 (107 total).

watcher-selftest.exe — all deterministic checks PASS (exit 0). Output identical to the
run-2 transcript above (including the "protection-degraded chip" kill-leg string and
the clean stop); the toast leg is again INCONCLUSIVE due to the machine-level banner
suppression (reports/M6-toast-probe.txt) — not a regression, does not gate the verdict.

## Run-3 artifacts — present
`F:\obsplugin\build_x64\RelWithDebInfo\` (2026-07-10 00:27):
`streamsentry.dll` (70,656 B, final PERF_LOG=OFF build) + `streamsentry.pdb`,
`coord-map-tests.exe`, `plate-gen-tests.exe`, `toast-gate-tests.exe`,
**`frame-decide-tests.exe` (new, 17,408 B + pdb)**, `watcher-selftest.exe`,
`w32-pthreads.dll`.

Deployment note (informational, per coordinator): the soak DLL at
`D:\software\obs\obs-studio\obs-plugins\64bit\streamsentry.dll` was refreshed again
tonight (2026-07-10 00:25, 73,216 B; pdb 1,765,376 B). Corroboration: it contains the
run-3 frame-decide reason string "detection degraded (monitor data unavailable)"
(grep -a hit = 1, same as the fresh build), its size equals the PERF_LOG=ON variant,
and its pdb grew vs the run-2 deployment (1,757,184 → 1,765,376 B), consistent with the
added module. Byte-level provenance beyond that is not machine-verified here.

## Addendum to coverage gaps
Run-1 and run-2 gap lists apply unchanged, with one improvement and one note:
- IMPROVED: the V6 failure mode (degraded state dropping confident masks) and the
  failure-reason precedence ordering are now covered by the frame-decide unit suite —
  this specific logic no longer relies on manual testing.
- The decision module is unit-tested against synthetic snapshots; the glue in
  `src/filter.c` that feeds real snapshot/geometry state into `ss_decide_frame()` and
  draws the result is still compile-verified only — in-OBS visuals (chip appearance,
  masks persisting while degraded) remain owner-manual items, as do the M6 soak /
  no-storm / real-toast rows and the ruling-provenance attestation.

## FINAL VERDICT (AUTHORITATIVE, for the run-3 staged tree identified above): VERIFIED
Clean zero-warning builds in both PERF_LOG variants (tree left at default OFF), ctest
4/4 PASS including the new V6-regression suite, watcher self-test deterministic legs
all PASS with the expected INCONCLUSIVE toast leg, all artifacts (incl.
frame-decide-tests.exe) present, soak deployment refresh corroborated.
